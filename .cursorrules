# Zellia (岁月安) - 全栈重构项目白皮书

## 1. 项目愿景与 UI/UX 核心原则
* **定位:** 面向 60 岁以上长辈的“极简健康执行助手”。
* **设计铁律:** * 绝对的极简主义，拒绝深层嵌套菜单。
  * **大字体、高对比度**，按钮触控区域必须大 (至少 48x48)。
  * 表单输入尽量使用原生选择器 (DatePicker, TimePicker, 标签选择) 代替键盘打字。
  * 数据删除一律采用**软删除 (Soft Delete)**，保留历史医疗档案。

## 2. 技术栈约束
* **后端:** Python 3.10+, FastAPI, SQLAlchemy, SQLite (开发用), Pydantic, JWT (OAuth2)。
* **前端:** Flutter (Dart), `http` (网络请求), `shared_preferences` (本地存储 Token), `intl` (日期处理)。

## 3. 全局数据库架构 (核心 5 表)
必须严格按照以下关系使用 SQLAlchemy 构建模型：
1. `users`: 核心用户表 (id, username, hashed_password)。
2. `medication_plans`: 用药计划引擎 (id, user_id, name, dosage, start_date, end_date, times_a_day[逗号分隔字符串], is_active[默认True])。
3. `medication_logs`: 实际打卡记录 (id, plan_id, user_id, taken_date, taken_time, is_taken)。
4. `blood_pressure_records`: 血压表 (id, user_id, systolic, diastolic, heart_rate, measured_at)。
5. `blood_sugar_records`: 血糖表 (id, user_id, level, condition, measured_at)。

## 4. 后端 API 规范 (RESTful + JWT)
* 所有的业务接口 (非 Auth) 必须依赖 `get_current_user` 校验 JWT Token。
* **Auth:** 提供 `/register` 和 `/login`。
* **用药模块:**
  * `POST /medications/plan`: 创建计划。
  * `GET /medications/today`: **核心智能接口**。需要根据当前日期，筛选出在 `start_date` 和 `end_date` 之间的 `medication_plans`，并根据 `times_a_day` 字段，将一天多次的药**拆分**成独立的待办方块返回。同时需要关联 `medication_logs` 查出当次是否已打卡。
  * `POST /medications/{plan_id}/log`: 提交/取消具体某天某时的打卡。
  * `DELETE /medications/plan/{plan_id}`: 将 `is_active` 设为 False (软删除)。
* **体征模块:** 提供血压和血糖的 `POST` (录入) 和 `GET` (获取最近 N 条历史) 接口。

## 5. 前端 Flutter 架构规范
* 采用标准的 `lib/screens`, `lib/services`, `lib/widgets` 目录结构。
* `api_service.dart` 统一管理所有网络请求，必须全局拦截 `401 Unauthorized` 状态码，自动清除本地 Token 并跳转回登录页。
* 核心页面 `TodayScreen`：
  * 上半部分：当天的用药打卡列表（使用 `ListView` + `Dismissible` 支持左滑软删除）。
  * 添加药物弹窗：必须使用 `StatefulBuilder`，结合原生的 `showDatePicker` 和动态可增删的 `ActionChip` / `InputChip` 来收集每天的多次服药时间。
  * 下半部分：大卡片样式的血压/血糖录入入口。